extends partials/_layout

block content
    h2 Start a game
    form(action="/join", method="post")
        if messages.error
            .error=messages.error
        input(type="text" name="name" value=name placeholder="Name")
        button.success(type="submit") Go